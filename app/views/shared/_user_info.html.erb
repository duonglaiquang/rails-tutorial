<%= link_to gravatar_for(current_user, size: Settings.user_setting.gravata_index_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("application.profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
